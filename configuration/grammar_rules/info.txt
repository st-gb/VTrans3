the file name matters: some definitions need to precede other definitions.
  so one may need to put numbers like "0" at the begin of the file name if 
  selecting multiple files for loading grammar rules inside the translator application.